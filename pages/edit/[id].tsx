import type { NextPage } from "next";
import styles from "../../styles/Details.module.scss";

const Edit: NextPage = () => {
  return <section className={styles.container}>Edit</section>;
};

export default Edit;
