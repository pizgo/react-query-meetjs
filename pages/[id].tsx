import type { NextPage } from "next";
import styles from "../styles/Details.module.scss";

const Details: NextPage = () => {
  return <section className={styles.container}>Details</section>;
};

export default Details;
